<template>
  <!--

    @for((attack, index) <- pokemon.pType.attacks.zipWithIndex) {
        <button onclick="onAttack(@(index + 1), '@pokemon.pType.pokemonArt.toString', @isTurn)" class="attack-button button-style fight-button small">@attack.toString</button>
    }
  -->
</template>